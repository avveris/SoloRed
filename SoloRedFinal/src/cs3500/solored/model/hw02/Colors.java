package cs3500.solored.model.hw02;

/**
 * Represents the colors of the cards as an enum, that way they
 * cannot be changed by the client.  They are in the model because they
 * are a truth to the game.  I'm using the ordinal of the enum (0-4) as well.
 */
public enum Colors {
  RED,
  ORANGE,
  BLUE,
  INDIGO,
  VIOLET
}
